# あえて PROPFIND を持たない構成。組み込みの dav モジュールには
# PROPFIND が無く、nginx-dav-ext-module を足さないと WebDAV にはならない。
server {
    listen 80;
    root /data;
    location / {
        dav_methods PUT DELETE MKCOL COPY MOVE;
        autoindex on;
    }
}
