# WebDAV で公開するホスト側ディレクトリ (読み取り専用でマウントされる)
DAV_SHARE_PATH=/srv/share

# ホスト側の待受アドレス。
# リバースプロキシやトンネルを同じホストで動かすなら loopback のままにする。
# 直接 LAN に晒す場合は 0.0.0.0 にしたうえで、必ず認証を設定すること。
DAV_BIND_ADDR=127.0.0.1
DAV_BIND_PORT=8080

# コンテナを動かす uid:gid (共有ディレクトリの所有者に合わせる)
DAV_UID=1000
DAV_GID=1000
